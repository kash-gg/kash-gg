# Write your MySQL query statement below
select p.product_name , sum(unit) as unit from Products p  left join orders o on p.product_id = o.product_id where YEAR(o.order_date)='2020' and MONTH(o.order_date)='02' group by product_name having sum(unit) >= 100;